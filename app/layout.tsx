import type React from "react"
import type { Metadata } from "next"
import { Inter, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _inter = Inter({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Elevare HR Solution - Kumaon's First HR Practical Training Institute",
  description:
    "Build your career with practical HR skills. 100% job-oriented training in HR Documentation, Payroll, Compliance, Recruitment & more.",
  keywords:
    "HR training, HR practical training, HR course, payroll training, recruitment training, Kashipur, Uttarakhand",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
